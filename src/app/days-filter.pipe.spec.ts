import { DaysFilterPipe } from './days-filter.pipe';

describe('DaysFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
