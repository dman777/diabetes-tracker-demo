import { StaggerAnimationDirective } from './stagger-animation.directive';

describe('StaggerAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new StaggerAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
